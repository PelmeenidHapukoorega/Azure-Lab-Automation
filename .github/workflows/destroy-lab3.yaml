name: Destroy Lab 3 - Database

on: 
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  destroy-database:
    runs-on: ubuntu-latest
    steps:
      - name: Log in to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Delete Resource Group
        uses: azure/CLI@v1
        with:
          inlineScript: |
            az group delete --name toy-database-rg --yes --no-wait
            echo "Nuke command sent for toy-database-rg."
